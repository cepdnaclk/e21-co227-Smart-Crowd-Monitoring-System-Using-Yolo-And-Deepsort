// Simple per-building thresholds configuration
// Keys are building IDs as strings to avoid number/string key mismatches.
// Adjust these values as needed. A default is used when a building isn't listed.

export const DEFAULT_THRESHOLD = 50;

export const THRESHOLDS = {
  "1": 50,
  "2": 50,
};
