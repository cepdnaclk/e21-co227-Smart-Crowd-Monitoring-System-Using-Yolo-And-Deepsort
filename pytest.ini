[pytest]
markers =
    ai: Optional tests that touch AI components (YOLO/DeepSort)
    slow: Potentially slow tests intended to be run on demand
    integration: Integration tests (API+DB, threads, streams)